#include<stdio.h>

       int pin;
       int  balance = 30000,deposit,withdraw,balance_check,input,balance_withdraw;
       int choice,option,option_opt,opt,opts;
       int another_transaction; 
      